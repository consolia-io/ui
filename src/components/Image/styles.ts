import { styled } from "../../stitches.config";

export const ImageStyled = styled("div", {
  height: "100%",
  overflow: "hidden",
  position: "relative",
  transition: "$default",
  width: "100%",
});
